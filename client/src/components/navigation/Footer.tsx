import React from 'react';
import styled from 'styled-components';

const Footer: React.FC = () => <StyledFooter></StyledFooter>;

export default Footer;

const StyledFooter = styled.div`
  padding: 24px;
`;
